{
  "name": "canofold-workspace",
  "private": true,
  "license": "MIT",
  "homepage": "https://canofold.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/canofold/canofold.git"
  },
  "bugs": {
    "url": "https://github.com/canofold/canofold/issues"
  },
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm --filter @canofold/markdown build && pnpm --filter canofold build && pnpm --filter @canofold/vite build && pnpm --filter @canofold/plugins build",
    "dev": "node scripts/dev/runWorkspace.mjs",
    "dev:debug": "node scripts/dev/runWorkspace.mjs --debug",
    "test": "pnpm build && vitest run",
    "test:coverage": "pnpm build && vitest run --coverage",
    "test:architecture": "pnpm build && node --test tests/architecture/*.test.mjs",
    "test:browser-consumer": "pnpm --filter @canofold/markdown build && pnpm --filter @canofold/plugins build && pnpm --filter @canofold/markdown test:browser && pnpm --filter @canofold/plugins test:browser",
    "test:packed-cli": "pnpm build && node scripts/test-packed-cli.mjs",
    "test:react-matrix": "pnpm --filter @canofold/markdown build && node scripts/test-react-consumer.mjs",
    "test:watch": "vitest",
    "format": "prettier --write \"**/*.{ts,tsx,mts,cts,js,mjs,cjs,json,css}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,mts,cts,js,mjs,cjs,json,css}\"",
    "typecheck": "pnpm --filter @canofold/markdown typecheck && pnpm --filter canofold typecheck && pnpm --filter @canofold/vite typecheck && pnpm --filter @canofold/plugins typecheck",
    "report:bundles": "node scripts/bundle-report.mjs",
    "report:package": "node scripts/package-report.mjs",
    "audit:dependencies": "pnpm audit --prod --audit-level high",
    "benchmark:markdown": "pnpm --filter @canofold/markdown build && node packages/markdown/scripts/benchmark.mjs",
    "benchmark:search-quality": "pnpm build && node scripts/benchmark-search-quality.mjs",
    "benchmark:enterprise": "pnpm build && node scripts/benchmark-enterprise.mjs",
    "test:release": "pnpm format:check && pnpm typecheck && pnpm audit:dependencies && pnpm build && pnpm exec vitest run --coverage && node --test tests/architecture/*.test.mjs && pnpm --filter @canofold/markdown test:browser && pnpm --filter @canofold/plugins test:browser && node scripts/test-packed-cli.mjs && pnpm report:bundles && pnpm report:package && node scripts/benchmark-search-quality.mjs && node scripts/benchmark-enterprise.mjs"
  },
  "devDependencies": {
    "@canofold/plugins": "workspace:*",
    "@types/node": "^26.6.1",
    "@vitest/coverage-v8": "5.0.1",
    "canofold": "workspace:*",
    "esbuild": "^0.28.2",
    "prettier": "3.9.6",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
